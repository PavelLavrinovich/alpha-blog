<h1>Articles</h1>

<p>
  <%= link_to 'New article', new_article_path %>
</p>
<% @articles.each do |article| %>
  <p>
    <%= article.title %>  <%= link_to 'Show', article_path(article) %> <%= link_to 'Update', edit_article_path(article) %>
  </p>
<% end %>